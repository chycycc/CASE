# CASE-EPCL V9 实验全生命周期追踪日志
## (Project V9: Large-Batch Production, Dense-MCP & Geometry Alignment)

---

## 0. V9 课题总览与硬件跨越背景

- **立项基底**：V8 全周期收官（旗舰版 V8.2 F 斩获历史最优生成质量：Test PPL **32.83**，Greedy Dist-2 **16.79%**，Sampling Unique **98.60%**，EMO_acc **40.97%**，Alignment **0.9514**）。
- **硬件升级环境**：从本地 RTX 3050Ti (4GB) 跨越至 AutoDL 云端单卡 NVIDIA GeForce RTX 4090D (24GB 显存, CUDA 12.1, PyTorch 2.1.0, Python 3.10)。
- **核心解决矛盾**：
  1. **小批次负样本池受限**：原 4GB 下单步 Batch=8，每步约 24~27 个情感类别未被抽样，MCP 动量更新稀疏，EMA 轨迹阶跃抖动；
  2. **强制 AMP 精度妥协**：4GB 下为防 OOM 必须依赖 FP16 混合精度；24GB 环境下彻底解绑，回归 FP32 原生全精度高保真梯度反向传播；
  3. **吞吐瓶颈突破**：正式训练吞吐由 0.70 it/s 暴增至 2.83 it/s（真实样本吞吐提升 16.1 倍）。
- **第一不可逾越红线**：
  **坚决捍卫对话生成质量，Test PPL ≤ 32.83 为全生命周期第一核心红线**。

---

## 1. 跨代演进全景指标与 V9 目标矩阵 (Metric Matrix)

| 核心评估维度 | CASE 原版 (ACL'23) | V7 终局 (Trial 3) | V8 黄金旗舰 (V8.2 F) | **V9 目标基准线** |
| :--- | :---: | :---: | :---: | :---: |
| **测试集 PPL ↓** | 35.37 | 32.97 | **32.83** | **≤ 32.83 (捍卫第一红线)** |
| **测试集 EMO_acc ↑** | 40.20% | 40.63% | **40.97%** | **≥ 41.50% (冲刺 42.0%)** |
| **测试集 EMO_loss ↓** | 2.2553 | 2.1962 | **2.2150** | **≤ 2.1500** |
| **原型质心对齐度 (Alignment) ↑** | 0.0000 | 0.0520 | **0.9514** | **≥ 0.9500 (牢固对齐)** |
| **类簇紧实度 (DBI) ↓** | 5.6536 | 3.7668 | **4.7803** | **≤ 4.5000** |
| **Greedy Dist-2 ↑** | 4.01% | 4.22% | **16.79%** | **≥ 16.50%** |
| **自适应核采样 Unique ↑** | 25.40% | 94.90% | **98.60%** | **≥ 98.50%** |

---

## 2. V9 全阶段试验对比总览表 (Trial Scorecard)

| 试验代号 | 核心架构与训练配置 | 最佳验证 PPL | 测试集 PPL | 测试集 EMO_acc | 测试集 EMO_loss | 原型对齐度 | 训练物理耗时 | 归档与维护状态 |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **V8.2 F 旗舰基线** | B=8, 4GB, FP16, Step=40k | 37.0876 | **32.83** | **40.97%** | 2.2150 | **0.9514** | ~18小时 | 黄金基准 (只读对照) |
| **V9 Trial 1 (首飞)** | 等效 B=64, 24GB, FP32, LR=3e-4, 误跑 42k 步 | 42.0 (Step 5k) | 61.3608 | 34.10% | 2.6167 | 未提取 (已退化) | 3h 11m | **淘劣/审计归档** (`save/v9_trial1`) |
| **V9 Trial 2 (重标定)** | 等效 B=64, 解除拦截死代码, check=500, max=10k, save=3k | *待跑* | *待测* | *待测* | *待测* | *待测* | 预估 ~30m | **下阶段执行目标** (`save/v9_trial2`) |

---

## 3. 实验详细追踪与淘劣复盘记录 (Trial Logs & Post-Mortem)

### [Phase 1] V9 Trial 1: 24GB 大批次全量生产首飞与严重过拟合审计
- **启动时间**：2026-09-22 11:42
- **结束时间**：2026-09-22 14:54（耗时 3 小时 11 分钟，总迭代 42,000 步后早停退出）
- **硬件算力**：AutoDL 西北 B 区，单卡 RTX 4090D (24GB 显存)
- **运行配置**：
  ```
  单步物理 Batch: 32
  梯度累加步数: 2 (等效全局全局 Batch = 64)
  数值精度: 原生 FP32 全精度
  初始学习率: LR = 0.0003
  优化器 Warmup: 2,000 步
  预训练轮次: 4 轮 (耗时 3 分 42 秒)
  原型更新机制: 密集动量原型 Dense-MCP (use_mcp=True, momentum=0.96)
  对比学习超参: lambda_epcl=0.07, arc_margin=0.30, arc_mode=cos
  分类头冻结机制: 自适应冻结 (Adaptive Freeze, min=8000, max=16000)
  验证评估间隔: check_iter = 2,000
  退火保护期: min_save_step = 4,500
  早停容忍度: patience = 10
  保存权重路径: save/v9_trial1/ (原 save/v9_24g_baseline/)
  全量测试集结果: results/v9/v9_trial1_results.txt
  ```

#### 一、实测指标与终局评测数据 (5,255 测试样本)
```tsv
EVAL	BOW_Loss	KL_Loss	MIM_Loss	CTX_Loss	PPL	STR_loss	STR_acc	EMO_loss	EMO_acc
test	5.4219	0.0443	1.1378	4.1168	61.3608	0.0000	0.0000	2.6167	0.3410
```
- **测试集生成 PPL**: **61.3608**（严重击穿 32.83 黄金红线）；
- **测试集情感准确率 EMO_acc**: **34.10%**（落后黄金基准 6.87 个百分点）；
- **测试集情感损失 EMO_loss**: **2.6167**（远高于正常收敛区间的 ≤ 2.20）。

#### 二、TensorBoard 5 大关键曲线数据事实提取

1. **`ppl` 曲线**：
   - 训练集困惑度 `ppl_train` 从 50+ 持续单调下探，在第 42k 步被彻底压缩至 **3.531**（记忆化特征）；
   - 验证集困惑度 `ppl_valid` 在 **Step 5,000 ~ 6,000 步探底至最低波谷（~42.0）**，随后掉头一路单调攀升恶化，至 42k 步飙升至 60+。
2. **`composite_score` 曲线**：
   - 在 **0 ~ 20,000 步之间完全空白**，首个数据点直到第 22k 步才被写入（起始分值 88）；
   - 随后一路恶化上冲至 170+，全无波谷，早停计数器连续单调累加。
3. **`emo_acc` 曲线**：
   - 训练集准确率 `emo_acc_train` 狂飙至 **85% ~ 95%**；
   - 验证集准确率 `emo_acc_valid` 在 **Step 5,000 ~ 6,000 步达到峰值 ~41.5%**（超越历史基线 40.97%），随后持续退化，在 42k 步跌至 **35.13%**（泛化鸿沟 Gap 达 50%）。
4. **`emo_loss` 曲线**：
   - 训练集损失在第 14k 步因自适应冻结机制生效而断崖归零（1.93e-146）；
   - 验证集损失在 **Step 5,000 ~ 6,000 步探底至 ~2.15**，后程反弹并在 14k 步后被钉死在 2.605。
5. **`epcl_loss` 曲线**：
   - 呈现极为理想且平滑的对数衰减形态，从 2.40 单调收敛至 **0.089**，全程无梯度爆炸或数值震荡。

#### 三、双重致命致因深度剖析 (Post-Mortem Analysis)

##### 1. [代码逻辑缺陷] 历史遗留 `iters = 20000` 强行拦截了成熟期黄金权重落盘
- **缺陷定位**：`main.py:63` 定义 `iters = 20000`，并在 `main.py:154-155` 中执行：
  ```python
  model.train()
  if n_iter < iters:
      continue
  ```
- **物理后果**：原版 ACL 2023 用于小批次慢预热的逻辑在重构时未被清理。在前 20,000 步内，每一次验证评估后的早停监控、复合评分记录以及 `model.save_model` 逻辑被全部无条件跳过；
- **直接恶果**：模型在 Step 5,000 ~ 6,000 步达到全生命周期最佳状态（PPL ~42, EMO_loss ~2.15, EMO_acc ~41.5%）时，系统**完全没有保存该权重**。直至第 22,000 步跨过 20,000 门槛后才开始保存，但此时模型已严重过拟合，连续 10 次未能突破 22k 步的分值，最终早停退出并拿第 42k 步的劣质权重进行了测试。

##### 2. [训练动力学错配] 批次放大 8 倍后未等比压缩训练步数，模型强跑 138 个 Epoch
- **物理尺度转换**：ED 训练集仅包含 19,533 个对话样本。
  - 在 4GB 环境（单步 8）下，1 个 Epoch 需 2,441 步，跑 40,000 步等价于 **约 16.4 个 Epoch**；
  - 在 24GB 环境（等效全局 Batch 64）下，1 个 Epoch 仅需 **305 步**；
  - 跑 42,000 步，相当于模型在同一个 1.9 万条样本的小数据集上反复强行训练了 **137.7 个 Epoch**！
- **过拟合铁证**：自回归语言模型在同一个文本集上迭代 138 轮，必然导致训练集 PPL 降至 3.53 的病态记忆化，词表概率分布极度尖锐，泛化能力彻底崩塌；
- **真实物理波谷验证**：模型在等效 Batch 64 下的最佳收敛点正好在 **Step 5,000 附近**（$5000 / 305 \approx 16.4$ 个 Epoch，与 V8 物理周期惊人一致！）。

#### 四、核心学术定论与积极信号
1. **密集动量原型（Dense-MCP, $\mu=0.96$）完全验证**：`epcl_loss` 光滑衰减至 0.089，证明在大批次充分负样本下，原型对比学习空间高度平稳收敛；
2. **细粒度分类潜能巨大**：在未过拟合的 5k~6k 步，验证集准确率达 **41.5%**、损失达 **2.15**，证明 V9 架构天花板显著高于 V8；
3. **失败可精准归因**：本次失利纯属步数物理换算失误与代码拦截死代码所致，算法本身无逻辑硬伤。

---

## 4. V9 Trial 2 整改方案与落地部署矩阵

针对 Trial 1 暴露的问题，Trial 2 实施以下彻底整改：

1. **彻底拔除拦截死代码**：删除 `main.py` 中的 `iters = 20000` 与 `if n_iter < iters: continue`，从第 1 步起全流程接管评估与保存；
2. **重标定大批次步数体系（适配 Batch 64）**：
   - 验证间隔从 2,000 步加密至 **500 步**（每 1.6 个 Epoch 密集采样，精准捕获 5,000 步波谷）；
   - 保护期前移至 **3,000 步**（避开前期预热震荡）；
   - 早停耐心 `patience` 设为 **6 次**（连续 3,000 步未提升自动退出）；
   - 最大总步数硬上限设为 **10,000 步**（约 32 个 Epoch 兜底，彻底杜绝跑 138 轮的灾难）；
3. **独立工程隔离管理**：
   - 实验命名解耦：`EXP_NAME=v9_trial2`；
   - 权重保存独立：`save/v9_trial2/`；
   - TensorBoard 独立：写入 `save/v9_trial2/`；
   - 日志独立：`logs/v9_trial2.log`；
   - 评测独立：`results/v9/v9_trial2_eval.txt`；
4. **训练耗时预估**：总步数从 42k 压缩至 ~6k 步，训练耗时将从 3 小时 11 分大幅缩减至 **25 ~ 30 分钟**。
